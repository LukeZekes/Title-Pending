using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NOTE : MonoBehaviour {

	///Everything Labelled with "Controller" in the name refers to player 2
    ///I originally intended for player 2 to be controlled by a controller, but that resulted in issues with aiming
    ///This applies to EVERYTHING that says "Controller" in the name, not just scripts
}
